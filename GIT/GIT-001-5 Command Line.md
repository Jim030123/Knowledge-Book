- The command line is the only place place can run all Git commands - most of the command-line version, can probably also figure out how to run the GUI version, while the opposite is not necessarily true.
- How to run the command-line version, can probably also figure out how to run the GUI version, while the opposite is not necessarily true.

# Installing Git
- Ubuntu
```bash
sudo apt install git-all
```
- Using Debian-based distribution (doc, html, info). these additional decencies are required
```bash
sudo apt-get install install-info
```