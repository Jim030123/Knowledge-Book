# ledAttachPin()
- Accept 2 arguments:
  1. GPIO that will output the signal
  2. Will generate the signal.
```cpp
ledAttachPin(GPIO, channel)
```
# ledcWrite()
- Accepts as argument the channel that is generating the PWM signal and duty signal.
```cpp
ledcWrite(channel, dutycycle)
```

